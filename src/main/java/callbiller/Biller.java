package callbiller;

import java.util.HashMap;
import java.util.Map;

public class Biller {

    //private Set<CallerBill> callers = new TreeSet<>(Comparator.comparingLong(CallerBill::getBillCents).reversed());
    // Mapping of each caller number (String) to their bill (in cents)
    //private Map<String, Long> callerBills = new HashMap<>();

    public void addCall(CallRecord call) {
        
    }
} 